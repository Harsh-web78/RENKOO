node_modules
.next
out
build
*.tsbuildinfo
next-env.d.ts
.env
.env*.local
*.log
.DS_Store
# Backend
apps/api/dist
apps/api/node_modules
apps/api/coverage
coverage
# Prisma: schema + migrations ARE tracked (deploy runs `migrate deploy`).
# Only the generated client (lives in node_modules/.prisma) is ignored.
